int game_update_and_render(void) {
    // TODO: Move to persistent game init storage so we don't load each frame
    //Image image = image_load("assets/images/Faces For A Dying Land/creep32.png");
    //image_free(&image);
}
